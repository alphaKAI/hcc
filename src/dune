(executable
  (name main)
  (public_name hcc)
  (libraries core)
  (preprocess (pps ppx_let ppx_jane ppx_deriving.show ppx_deriving.eq ppx_deriving.ord)))
(env
 (dev
  (flags (:standard -warn-error -a))))
